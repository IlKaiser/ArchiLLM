package com.example.cart.domain.event;

import java.io.Serializable;

public abstract class CartEvent implements Serializable {
    // Marker base class for all Cart domain events

    // Optionally, add event metadata fields (e.g., timestamp, eventId) if needed
    // TODO: Add common event fields if required
}
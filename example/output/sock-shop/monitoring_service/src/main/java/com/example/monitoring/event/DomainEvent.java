package com.example.monitoring.event;

/**
 * Marker interface for domain events consumed by the MonitoringService.
 */
public interface DomainEvent {
    // TODO: Add common event methods if needed (e.g., getEventType(), getTimestamp())
}